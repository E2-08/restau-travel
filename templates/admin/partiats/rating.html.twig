{% for i in 1..5 %}
    {% if i <= rating %}
        {# {% if i == 0.5 %} 
            <i class="fas fa-star-half"></i>
        {# {% else %} #}
            <i class="fas fa-star"></i>
        {# {% endif %} #}
  
    {% else %}
        <i class="far fa-star"></i>

    {% endif %}

{% endfor %}