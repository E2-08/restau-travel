{% extends '/admin/base.html.twig' %}
{% block title %}Modification du compte itulisateur{% endblock %}
{% block body %}
    <div class="container">
        <div class="jumbotron"></div>
        {{form_start(form)}}
        <div class="form-group">
            <div class="col">
                {{form_row(form.firstName)}}
                {{form_row(form.firstName)}}
            </div>

            <div class="col">
                {{form_row(form.firstName)}}
                {{form_row(form.firstName)}}
            </div>
        </div>
        {{form_widget(form)}}
        <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
        {{form_end(form)}}
    </div>
{% endblock %}